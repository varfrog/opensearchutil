{{define "mapping_property"}}
  "{{.FieldName}}": {
      {{if .FieldType}}
        "type": "{{.FieldType}}"
      {{end}}
      {{if .Children}}
          "properties": {
            {{$length := len .Children}}
            {{range $index, $item := .Children}}
                {{template "mapping_property" $item}}
                {{if (notLast $index $length)}},{{end}}
            {{end}}
          }
      {{end}}
  }
{{end}}

{
  "settings": {
    "number_of_shards": 1,
    "number_of_replicas": 2
  },
  "mappings": {
    "properties": {
      {{$length := len .MappingProperties}}
      {{range $index, $item := .MappingProperties}}
          {{template "mapping_property" $item}}
          {{if (notLast $index $length)}},{{end}}
      {{end}}
    }
  }
}
