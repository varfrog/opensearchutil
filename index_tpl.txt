{
  "settings": {
    "number_of_shards": 1,
    "number_of_replicas": 2
  },
  "mappings": {
    "properties": {
      {{range .MappingProperties}}
          "{{.FieldName}}": {
              "type": "{{.FieldType}}"
          }
      {{end}}
    }
  }
}
